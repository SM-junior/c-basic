#include <stdio.h>

int main()
{

    // int a, b;

    // scanf("%d", &a); // wait for user integer input
    // scanf("%d", &b); // wait for user integer input

    // printf("%d %d\n", a, b);

    // int c, d;
    // scanf("%d %d", &c, &d);
    // printf("%d %d\n", c, d);

    float e, f;
    scanf("%f %f", &e, &f);  // wait for user float input
    printf("%f %f\n", e, f); // wait for user float input

    return 0;
}
